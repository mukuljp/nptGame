import { RoomArrayFilterPipe } from './room-array-filter.pipe';

describe('RoomArrayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomArrayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
